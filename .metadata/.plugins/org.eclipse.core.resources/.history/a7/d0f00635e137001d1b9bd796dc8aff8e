package sec1;

import s2.Week;

// public : 현재 프로젝트내에서 다른 패키지에서도 접근 가능
// default : 접근 제한자를 생략했을 경우이며, 같은 패지키 내에서 접근 가능
// provate : 현재 클래스 내부에서만 접근 가능 
public class Member {
	String id; // 클래스의 멤버 필드
	String password;
	String name;
	String address;
	String tel;
	String reg_date;

}
